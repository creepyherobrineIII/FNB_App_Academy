# range() function

numbers = range(5)
for number in numbers:
    print(number)

# With additional parameters
for number in range(5, 10, 2): # range (starting_val, ending_val(exclu), skip_val)
    print(number) 
