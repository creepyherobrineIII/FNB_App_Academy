# Strings
course = 'Python for beginners'

# Strings are immutable
print(course.upper())
print(course.find('o')) # Returns index of the first occurence of the char(s)
print(course.replace('for', '4')) # Replaces chars in a string with other chars

print('Python' in course) # Returns boolean stating whether a sequence exists in a variable or sequence